# Core FastAPI + server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database (SQLAlchemy 2.x async for the app; psycopg2 stays for Alembic --
# see app/core/database.py and alembic/env.py, HANDOFF-02 Fix A)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# Environment & config
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# Security (password hashing + JWT)
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Optional but very useful for dev
alembic>=1.13.0          # migrations (we'll set up soon)
httpx>=0.27.0            # for testing API calls
pytest>=8.0.0            # for tests (add later)
pytest-asyncio>=0.23.0   # async test functions + fixtures (HANDOFF-02)

email-validator>=2.0.0.post1   # for email validation in Pydantic
argon2-cffi   #I added this due to the 72 bcrypt error

fastapi-mail>=1.4.1
pydantic[email]

google-cloud-storage

boto3>=1.34.0   # Cloudflare R2 (S3-compatible) presigned uploads

pyyaml>=6.0     # grading engine params loader (app/underwriting/grading/params.py)